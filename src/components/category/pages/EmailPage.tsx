import React from "react";
import Template from "../../template/Template";
import EmailBody from "../body/EmailBody";

const EmailPage: React.FC = () => {
  return <Template>{<EmailBody />}</Template>;
};
export default EmailPage;
