import React from "react";
import Template from "../../template/Template";
import ServerBody from "../body/ServerBody";

const ServerPage: React.FC = () => {
  return <Template>{<ServerBody />}</Template>;
};
export default ServerPage;
